export {default as SwiperImage} from './SwiperImage'
export {default as SwiperVideo} from './SwiperVideo'
{
  /* <Swiper
            loop={false}
            style={{height: 330}}
            showsPagination={false}
            onIndexChanged={onChangeIdCurrent}
            index={0}>
            {item.arrImage?.map((src, j) => (
              <ViewCore
                key={j}
                width={'100%'}
                height={300}
                backgroundColor='#ddd'>
                <ImageCore source={uriImg(src)} width={'100%'} height={300} />
              </ViewCore>
            ))}
          </Swiper>
          <ItemBadge ref={refBadge} quantity={item.arrImage?.length} /> */
}
